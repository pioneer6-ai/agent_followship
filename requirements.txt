# Patient Follow-up Agent - Python Dependencies

# Core Framework
flask==3.0.0
werkzeug==3.0.1

# Date/Time handling
python-dateutil==2.8.2

# For future LLM integration (optional)
# openai==1.3.0
# Required for LIVE Claude usage: the tools/ tool-use loop AND the agent's
# DECIDE step (agent/decision.py). Everything else, including the offline demo
# and the whole test suite, runs without it -- the import is lazy and the agent
# silently falls back to its rule engine when it is missing.
# anthropic==0.40.0

# For production SMS/Communication (optional, not needed for demo)
# twilio==8.11.0
# python-whatsapp==0.0.1

# AWS messaging backends for the send_sms / send_email tools (AWS End User
# Messaging SMS + Amazon SES). Imported lazily: without it those two tools
# report error_code "config_missing" and everything else keeps working.
boto3==1.35.36

# Development and Testing
pytest==7.4.3
pytest-cov==4.1.0

# TLS trust store for outbound HTTPS/SMTP. Optional at import time (tools/tls.py
# falls back to the system store), but a macOS python.org install ships no CAs
# until "Install Certificates" is run, so every live send fails with
# CERTIFICATE_VERIFY_FAILED without it.
certifi==2026.7.22

# Code Quality
flake8==6.1.0
black==23.12.1

# Type Checking
mypy==1.7.1

# Documentation
sphinx==7.2.6
